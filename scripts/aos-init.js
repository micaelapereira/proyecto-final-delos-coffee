// ============================================================
// Inicialización de AOS (Animate On Scroll) — animaciones sutiles
// al hacer scroll, compartidas por las 5 páginas del sitio.
// ============================================================
AOS.init({
    duration: 700,
    easing: "ease-out-cubic",
    once: true,
    offset: 60,
});
